# =============================================================================
# FORGEFLOW MASTER — registered repos (Blueprint v1.3 Section 7, Board N5)
# Home: github.com/chromasmith/forgeflow  master/registered-repos.yaml
#
# THE SINGLE AUTHORITY on which repos are rendered from the master and from which
# master version. Written LAST — only after a render has landed and been proven
# (blob SHAs equal, render.py check OK). A propagation pass updates it once, at
# the end, never row by row mid-pass (PROPAGATION_PROCEDURE.md step 5).
#
# state: current  — rendered from the master version named here
#        pinned   — deliberately lagging; `reason` is mandatory and is what the
#                   protocol_drift line in the repo's handoff.yaml is derived from
# config_hash is the full SHA-256 render.py stamps into the rendered headers.
# A repo not listed here is NOT rendered from the master, whatever its files say.
# Per RULING-001/002 (.forge/rulings.yaml) rows are added one at a time, each on
# Matt naming the repo; there is no queue here and no implied order.
# =============================================================================

master_version_current: "v1.3.1"   # landed 2026-09-07 (5664d1b) and PROPAGATED to all nine rows the same day by the local run FF-V131-MASTER: every row below is current at v1.3.1, no repo is pinned or lagging. v1.3.1 repaired FF-013/FF-014/FF-015 — the defects that running v1.3.0 across nine real repos exposed.

repos:
  - repo: chromasmith/seogeo
    config: .forge/protocol-config.yaml
    profile: docs-only
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: 2b2e5be3e7fc2cf3a48a93a3ef255fc45f441159bfeee92ec1fd219770041412
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 8c91c0d (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. Previous: v1.3.0 propagation 2026-09-07: ONE commit 9b729af (all six files) by the local Claude Code run PROPAGATE-v1.3.0; every blob SHA equal to the Claude Web sandbox render and re-proven through the connector after the push. Previous: v1.1.0 re-render 2026-09-05: commits 1746d6c (config), ff60fc1 (CLAUDE.md), b504fb8 (end.dispatch NEW), 40e9040 (start.dispatch NEW), 30be55b (end), a94d846 (start); blob SHAs equal to the sandbox output. Previous v1.0.0 render: 042427f, 6c792a0, 14f1614 (2026-09-04)"
    notes: >-
      First repo ever rendered from the master (RULING-002). THIRD repo re-rendered from
      v1.1.0 with the full harness: claude.yml installed by Matt via the web editor (blob
      4cccfbf = master/assets/claude.yml), CLAUDE_CODE_OAUTH_TOKEN set from 1Password, branch
      protection on main with admin bypass (RULING-011). Private repo: detection ran on a tree
      skeleton rebuilt from the GitHub listing, carrying the REAL claude.yml and CLAUDE.md
      (blob SHAs matched before render). No subsystems detected; dispatch=on with
      allowed_tools read from the workflow; the repo's own SEO/GEO template payload under
      template/.seogeo/ is correctly NOT treated as an installed tool. CLAUDE.md is the house
      block alone (no repo notes outside the markers). north_star_doc = .forge/BLUEPRINT.yaml,
      backlog prefix BL-.
  - repo: chromasmith/chromasmith-saas-starter
    config: .forge/protocol-config.yaml
    profile: standard
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: b77403904a76c6c81a826eb41713929bc8d37b541bdee09d73f6e48c9015f7c9
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 84fdc46 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. Previous: v1.3.0 propagation 2026-09-07: ONE commit 9f50d33 (all six files) by the local Claude Code run PROPAGATE-v1.3.0; every blob SHA equal to the sandbox render and re-proven through the connector after the push. Previous: v1.1.0 re-render 2026-09-05: commits 1f42125 (config), b742dd2 (CLAUDE.md), fda3547 (end.dispatch NEW), 028eea9 (start.dispatch NEW), d96fd69 (end), f3110b0 (start); blob SHAs equal to the sandbox output. Previous renders: v1.0.0 first render a296c13/10bb67e/60c0906/c97a49a, v1.0.0 re-render after SEO/GEO b750941/060c1c5/c6920b1/7b84b45 (2026-09-04)"
    notes: >-
      Second repo rendered (Matt named it 2026-09-04). This repo IS THE TEMPLATE, so its
      rendered files are inherited by every project spawned from it; a child must re-render
      with its own config (repo.name, and drop the build.deploy override once linked to
      Vercel) AND install its own harness (claude.yml is inherited by the clone, but the
      CLAUDE_CODE_OAUTH_TOKEN secret and branch protection are per-repo settings that do not
      copy). FOURTH repo re-rendered from v1.1.0 (RULING-012 moved it ahead of the Synclips
      trio; its SEO/GEO install session was confirmed wrapped first): claude.yml installed by
      Matt via the web editor (blob 4cccfbf = master/assets/claude.yml), CLAUDE_CODE_OAUTH_TOKEN
      set from 1Password, branch protection on main with admin bypass (RULING-011). Private
      repo: detection ran on a tree skeleton rebuilt from the GitHub listing, carrying the REAL
      claude.yml and CLAUDE.md (blob SHAs matched before render). Detected: single-app,
      npm-build, supabase, ci_quality_gate (quality-gate.yml), chromaqa (chromaqa-check.sh, a
      real running gate here), seogeo (.seogeo/config.json), tsx_lib_rule, dispatch=on with
      allowed_tools read from the workflow; vercel off. Override build.deploy: none because the
      template itself is deployed nowhere. Note: .chromaqa/profiles.json is still absent there
      (the seogeo generator does not yet emit the keys ChromaQA 7.0 reads — tracked in seogeo's
      handoff), so the S-090 generated-profile rule describes a state the template has not
      reached yet. north_star_doc = START_HERE.md, backlog prefix OBS-.
  - repo: chromasmith/forgeflow
    config: .forge/protocol-config.yaml
    profile: docs-only
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: 7ccf78dfab2fd1a61cc67fe1d8e97cd796b6de197a1a560da45381988267b46d
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 7c41e86 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. Previous: v1.3.0 re-render 2026-09-07: ONE commit e4466c4 (all six files + inbox FF-V130-MASTER) by a local Claude Code run, right after the master change 8db4ff2 (both via apply script blob e2e23c85 on tmp/v130-apply); all six blob SHAs equal to the sandbox output; render.py check OK and tests 60/60 from a fresh clone at e4466c4. Previous: v1.2.0 re-render 8ec708c (2026-09-06); v1.1.0 re-render e60eff5 / 94a5247 / 5de80fc / a0aa16a / 173aa3d / f9b8d33 (2026-09-05); v1.0.0 render 0f7ec5e / 5e1f463 / 56f2389 / b9046e2 (2026-09-04)"
    notes: >-
      Third repo rendered (RULING-004) and the master's own home: master/ is the source,
      .forge/protocols/ the output; a render never reads .forge/protocols/, so rendering the
      repo from itself is not circular. FIRST repo re-rendered from v1.1.0 (RULING-009) and
      the first with the full dispatch harness: .github/workflows/claude.yml installed by Matt
      via the web editor (blob 4cccfbf = master/assets/claude.yml), CLAUDE_CODE_OAUTH_TOKEN
      set from 1Password, branch protection on main with admin bypass (RULING-011, bypass
      proven by direct docs commit be2d757). Public repo, detection on the real tree:
      dispatch=on, allowed_tools read from the workflow, no build/deploy/subsystems. The rest
      of CLAUDE.md above the house block is still the unfilled Chromasmith template.
      north_star_doc = master/BLUEPRINT.yaml, backlog prefix FF-. FIRST and so far ONLY repo on
      v1.2.0 (RULING-013, internal-only session 2026-09-06): S-003 now excluded here (no
      artifact register), S-078 gone, W-005 in; the master change and the re-render both landed
      through a local Claude Code run reading a self-verifying apply script from a temporary
      branch — the connector cannot carry the 110 KB start master and a base64 blob in a
      Claude Web reply arrived corrupted (see GOTCHAS).
  - repo: chromasmith/chromaqa
    config: .forge/protocol-config.yaml
    profile: docs-only
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: ff50e23e6bcdbe931aa2c0943ddac222fd7f15932982284ffee26ed1270b8221
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 2611905 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. Previous: v1.3.0 propagation 2026-09-07: ONE commit e72a6a6 (all six files) by the local Claude Code run PROPAGATE-v1.3.0b; the first pass HALTED here on a config_hash mismatch (b5c5ed08 expected vs ff50e23e actual) because .forge/protocols/artifact-register.yaml was added to the repo after the sandbox snapshot was taken — Claude Web re-rendered against the current tree and the second pass matched every SHA first try. Previous: v1.1.0 re-render 2026-09-05: commits 3fb7e4f (config), ab9364b (end.dispatch NEW), 67031b3 (CLAUDE.md), b9152c4 (start.dispatch NEW), a868ed2 (end), 9263d5b (start); blob SHAs equal to the sandbox output. Previous v1.0.0 render: 2d9980e / ac7faac / de3f45f / 8f52dc8 (2026-09-04)"
    notes: >-
      Fourth repo rendered (RULING-005/006) — the ChromaQA tool's own repo, which RUNS ChromaQA
      ON ITSELF, so subsystems.chromaqa is detected TRUE and not overridden. SECOND repo
      re-rendered from v1.1.0 with the full harness: claude.yml installed by Matt via the web
      editor (blob 4cccfbf = master/assets/claude.yml), CLAUDE_CODE_OAUTH_TOKEN set from
      1Password, branch protection on main with admin bypass (RULING-011). Private repo:
      detection ran on a tree skeleton rebuilt from the GitHub listing, carrying the REAL
      claude.yml and CLAUDE.md (blob SHAs matched before render). Detected: single-app, no
      build (bash script + docs), ci_quality_gate, chromaqa, dispatch=on with allowed_tools
      read from the workflow; supabase/vercel/seogeo/ui_inventory off. The house-block ChromaQA
      line now reads correctly for a repo with no installer section (FF-001 closed here).
      north_star_doc = .forge/plans/chromaqa-modernization-plan.yaml, backlog prefix CQA-.
      Its legacy backlog shape migrates at the repo's first wrap on the rendered protocol.
  - repo: chromasmith/synclips-platform
    config: .forge/protocol-config.yaml
    profile: standard
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: 311239e39f25a12459e7f5186f2af3dcca656f5c10bf72c9cffa3564d383eb9b
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 5363d41 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. DISPATCH ON for the first time here (Matt installed claude.yml via the web editor 2026-09-07, blob 4cccfbf = the master asset), so the two .dispatch.yaml reader files are NEW in this commit. Previous: v1.3.0 propagation 2026-09-07: ONE commit 73e84a6 (four files; dispatch still OFF, no harness) by the local Claude Code run PROPAGATE-v1.3.0; blob SHAs equal and re-proven through the connector. Previous: commits 814578a (config), c383bd2 (start), 09d6c1e (end), 4c54dd5 (CLAUDE.md), all 2026-09-04; blob SHAs equal; render.py check OK from a fresh forgeflow clone"
    notes: >-
      Fifth repo rendered (Matt named it 2026-09-04, RULING-007) — the first Synclips-system
      repo and the first MONOREPO. companion_repos declares synclips-relay (role relay,
      PROTOCOL.md confirmed present in that repo's tree), so S-015/S-055 and E-020/E-044
      render in. Private repo: detection ran on a tree skeleton rebuilt from the GitHub
      listing. Detected: supabase, chromaqa (chromaqa-check.sh + .chromaqa/, a real running
      gate), ci_quality_gate (quality-gate.yml); seogeo/ui_inventory off; dispatch ON since
      2026-09-07. FOUR OVERRIDES, all because P3 detection reads a monorepo tree wrong:
      shape.kind monorepo + shape.build_location apps/admin (root package.json is only the
      workspaces root); subsystems.vercel true + build.deploy vercel (vercel.json lives in
      apps/admin); architecture.tsx_lib_rule true (.tsx tree is apps/admin/src). Also
      repo.line_endings mixed (CLAUDE.md standing rule), build.deploy_verify pinned to the one
      true Vercel project/team IDs from CLAUDE.md, north_star_doc =
      .forge/blueprint/MANIFEST.yaml (the 53 KB platform blueprint is marked do-not-read),
      backlog prefix SC-. House block APPENDED to CLAUDE.md (no markers). Legacy .forge/ shape
      (backlog on the old schema, no rulings.yaml, no artifact register, no inbox/, GOTCHAS at
      140 KB) migrates at the repo's first wrap on the rendered protocol, not at the render.
  - repo: chromasmith/chromasync
    config: .forge/protocol-config.yaml
    profile: standard
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: 2bde1ab3c7d3790cc11a9c9883b338d38be8f9da4d844cfe81b89ee420fe24f0
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 502f346 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. DISPATCH ON for the first time here (Matt installed claude.yml via the web editor 2026-09-07, blob 4cccfbf = the master asset), so the two .dispatch.yaml reader files are NEW in this commit. Previous: v1.3.0 propagation 2026-09-07: ONE commit 8e53d33 (four files; dispatch still OFF, no harness) by the local Claude Code run PROPAGATE-v1.3.0; blob SHAs equal and re-proven through the connector. Previous: commits 0d5efb3 (config), 3fd099b (start), 6f726cd (end), e4b455e (CLAUDE.md), all 2026-09-04; blob SHAs equal; render.py check OK from a fresh forgeflow clone"
    notes: >-
      Sixth repo rendered (Matt named it 2026-09-04, RULING-008) — second Synclips-system
      repo, companion synclips-relay. A TWO-LANGUAGE repo: Python desktop agent (src/, built
      into ChromaSync.exe by the watcher on push, swapped per machine by hand — never a
      session step) plus a Next.js web UI (web/, on Vercel; root package.json delegates to
      web/, root vercel.json carries the ignoreCommand). Private repo: detection ran on a
      tree skeleton rebuilt from the GitHub listing. Detected: single-app at root,
      npm-build, vercel, chromaqa (chromaqa-check.sh, v6.1 — its .chromaqa/ telemetry is
      GITIGNORED here, so S-014's session-start debt read finds nothing on GitHub; a v6.1
      fact, noted in the config), ci_quality_gate; supabase/seogeo/ui_inventory off (no
      supabase/ dir — the repo uses Supabase at runtime only); dispatch ON since 2026-09-07.
      One override: architecture.tsx_lib_rule true (the .tsx tree is web/, invisible to P3's
      root src/ glob). No north_star_doc — the only vision doc (60 KB blueprint) is marked
      do-not-read. Backlog prefix CS-, line endings lf (probed: .forge/handoff.yaml hashes
      LF). House block APPENDED to CLAUDE.md (no markers). Master follow-up worth filing:
      no validation knob fits a Python+web repo (npm-build runs the web build for
      Python-only changes; py-compile's block assumes a .venv this repo does not have).
  - repo: chromasmith/dv-captain
    config: .forge/protocol-config.yaml
    profile: docs-only
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: b25fff229f267700c0bb9ad04c22427778111315a85f4385b9dce72efd1d3b57
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit c22d068 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. DISPATCH ON for the first time here (Matt installed claude.yml via the web editor 2026-09-07, blob 4cccfbf = the master asset), so the two .dispatch.yaml reader files are NEW in this commit. Previous: v1.3.0 propagation 2026-09-07: ONE commit 607aeb5 (four files; dispatch still OFF, no harness) by the local Claude Code run PROPAGATE-v1.3.0b; the first pass HALTED here on a config_hash mismatch caused by a local-only .venv flipping build.validation to py-compile, so build.validation is now a DECLARED override and the file renders identically with or without it. Previous: commits eab16f9 (config), 0a03a56 (start), 4444fa6 (end), 4f7a128 (CLAUDE.md), all 2026-09-04; blob SHAs equal; render.py check OK from a fresh forgeflow clone at 5119307"
    notes: >-
      Seventh repo rendered (Matt named it 2026-09-04, RULING-008) — third and last of the
      Synclips trio, companion synclips-relay (the relay itself is never rendered). PURE
      PYTHON code repo with NO CI by design (CLAUDE.md standing rule), no ChromaQA, no
      Vercel, no package.json. Profile docs-only chosen ONLY for its knob values (complete
      mirror, hard reset, validation none, deploy none); it is not a docs repo. Private
      repo: detection ran on a tree skeleton rebuilt from the GitHub listing; nothing
      detected besides the harness. build.validation none is now a DECLARED override, not a
      detection: a .venv exists in the local checkout but not on GitHub, so detection read
      the repo two different ways depending on where it ran; either way the master's
      py-compile block (S-082) demands a .venv interpreter and this repo runs on system
      Python312 on the canonical machine (install.py), so task prompts name their own
      compile/import check. build.deploy none because Anvil daemons are redeployed by hand
      from deploy/anvil-wrappers and LodeKeep workers are not a session step. Declared:
      repo.alternate_machines [BM-Anvil, LodeKeep]; repo.line_endings mixed (.gitattributes
      forces LF everywhere except *.ps1 = CRLF, so "lf" would invite a fix); north_star_doc
      .forge/VISION.md; backlog prefix BACKLOG-. House block APPENDED to CLAUDE.md (no
      markers). Legacy .forge/ shape (GOTCHAS 182 KB, old backlog schema, no rulings.yaml,
      no artifact register, no inbox/) migrates at the repo's first wrap. Master follow-up
      (same as chromasync): a Python validation knob that works without a .venv.
  - repo: chromasmith/convergible-sandbox
    config: .forge/protocol-config.yaml
    profile: standard
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: 75e4a365bd50137143bb482aa1da9d490384047f01065002125b7468ebbf52a8
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit c66bd07 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. Previous: FIRST RENDER 2026-09-07: ONE commit 2fe1cce by the local Claude Code run PROPAGATE-v1.3.0 — six rendered files, the four hand-written protocol files DELETED (start/end x local/dispatch), and CC-reports/ renamed to .forge/inbox/ with git mv in the same commit; blob SHAs equal to the sandbox render and re-proven through the connector."
    notes: >-
      EIGHTH repo rendered and the master's own lineage source: the dispatch blocks, the
      artifact register, the rulings register and the one-item agenda were all reconciled
      out of this repo's four hand-written protocol files, which that render replaced.
      Named by Matt under RULING-012 and rendered in the FF-009 propagation rather than a
      session of its own (his call, 2026-09-07). Private repo: detection ran on a tree
      skeleton rebuilt from the GitHub listing carrying the REAL claude.yml — this repo's
      claude.yml is the hand-written 2026-08-29 CONVERGIBLE OVERLAY (PG env vars for
      read-only database access, psql and bunx depcheck on the allowlist) and is NEVER
      replaced by master/assets/claude.yml; render.py read its allowlist from that file.
      Detected: single-app npm-build, supabase, vercel (vercel.json at root) so
      build.deploy vercel, ci_quality_gate, ui_inventory (.forge/specs/ui-inventory.yaml),
      artifact_register (.forge/protocols/artifact-register.yaml), tsx_lib_rule,
      dispatch=on. No ChromaQA and no SEO/GEO here. One override:
      subsystems.database_access_doc = .forge/protocols/database-access.yaml, binding on
      any dispatched task that reads data. north_star_doc =
      .forge/specs/convergible-2.5-vision.yaml, backlog prefix CV-. STILL OWED:
      .forge/repo-rules.yaml (the DOC-FOLD-1 items) has not been written, so
      subsystems.repo_rules_doc is unset and S-005 does not render; when that file exists,
      set the knob and re-render. Legacy .forge/ shape migrates at the repo's first wrap.
  - repo: chromasmith/convergible
    config: .forge/protocol-config.yaml
    profile: standard
    master_version: "v1.3.1"
    rendered_on: 2026-09-07
    config_hash: 148571eab9cb6f7e34c49cef935a17bdbfe9871369791ae4148e6df511f08bdf
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/start-protocol.dispatch.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocols/end-protocol.dispatch.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "v1.3.1 propagation 2026-09-07: ONE commit 264bd50 (six files) by the local Claude Code run FF-V131-MASTER; all six blob SHAs equal to the Claude Web sandbox render and re-proven through the connector after the push. Config rewritten in the same commit: subsystems.vercel / build.deploy / build.deploy_verify are now DECLARED (the Vercel link is a dashboard setting and puts no file in the tree), pinned to project prj_YaDByQ4SmJy8GWgCY6z2mQ1ZLCYZ on team team_t36B0aZYLYFBbYAqNSi37S98, Root Directory apps/web — CV-OPEN-3 closed. Previous: FIRST RENDER 2026-09-07: ONE commit 6b08096 (all six files) by the local Claude Code run PROPAGATE-v1.3.0b, on top of 3cb595b which committed the ChromaQA debt ledger the gate had appended locally; blob SHAs equal to the sandbox render and re-proven through the connector. The first pass SKIPPED this repo because that ledger left the checkout dirty."
    notes: >-
      NINTH repo rendered — the production Convergible app, spawned from
      chromasmith-saas-starter on 2026-09-05. Until its first render it carried the
      TEMPLATE's protocol-config.yaml and rendered protocols VERBATIM (blob-identical,
      repo.name chromasmith-saas-starter, canonical checkout
      C:\Chromasmith\chromasmith-saas-starter, OBS- prefix, north_star START_HERE.md which
      does not exist here), so every session opening this repo was briefed about the
      template it came from — the spawn re-render step in START_HERE was never run. Private
      repo: detection ran on a tree skeleton rebuilt from the GitHub listing carrying the
      REAL claude.yml (blob 4cccfbf = the generic asset, inherited by the spawn). Detected:
      supabase, ci_quality_gate, chromaqa (7.1 here), seogeo (.seogeo/config.json),
      dispatch=on. Declared, not detected: shape.kind monorepo + shape.build_location
      apps/web (the root package.json only proxies scripts into the workspace, repo ruling
      CV-R4), architecture.tsx_lib_rule true (.tsx lives under apps/web/src, invisible to
      P3's root globs), and — since 2026-09-07 — subsystems.vercel, build.deploy and
      build.deploy_verify, because linking Vercel from the dashboard puts no file in the
      tree for detection to find. Root Directory is apps/web; setting it wrong ships the
      site noindex with a green build (repo trap CV-T4). NOT SET: north_star_doc —
      Convergible has no product-truth document in this repo yet (.forge/specs/blueprint.md
      is still the template's). Backlog prefix CV-. ALSO OWED BY MATT (repo handoff
      CV-OPEN-4): the CLAUDE_CODE_OAUTH_TOKEN secret and branch protection, neither of which
      copies with a spawn — until the secret is set, the rendered protocol offers a dispatch
      lane the repo cannot actually run.
