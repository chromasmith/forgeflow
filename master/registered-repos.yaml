# =============================================================================
# FORGEFLOW MASTER — registered repos (Blueprint v1.3 Section 7, Board N5)
# Home: github.com/chromasmith/forgeflow  master/registered-repos.yaml
#
# THE SINGLE AUTHORITY on which repos are rendered from the master and from which
# master version. Written LAST — only after a render has landed and been proven
# (blob SHAs equal, render.py check OK). A propagation pass updates it once, at
# the end, never row by row mid-pass (PROPAGATION_PROCEDURE.md step 5).
#
# state: current  — rendered from the master version named here
#        pinned   — deliberately lagging; `reason` is mandatory and is what the
#                   protocol_drift line in the repo's handoff.yaml is derived from
# config_hash is the full SHA-256 render.py stamps into the rendered headers.
# A repo not listed here is NOT rendered from the master, whatever its files say.
# Per RULING-001/002 (.forge/rulings.yaml) rows are added one at a time, each on
# Matt naming the repo; there is no queue here and no implied order.
# =============================================================================

master_version_current: "v1.0.0"

repos:
  - repo: chromasmith/seogeo
    config: .forge/protocol-config.yaml
    profile: docs-only
    master_version: "v1.0.0"
    rendered_on: 2026-09-04
    config_hash: 81ca6a54d712181be0d895f1411d7e4c88887ecde64ed8237800c4eeb4a7e23c
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "commits 042427f, 6c792a0, 14f1614 (2026-09-04); render.py check OK from a fresh forgeflow clone"
    notes: >-
      First repo ever rendered from the master (RULING-002). Private repo: detection ran on a
      tree skeleton rebuilt from the GitHub listing (see RENDER_PROCEDURE.md STATUS). No
      subsystems detected; dispatch off; the repo's own SEO/GEO template payload under
      template/.seogeo/ is correctly NOT treated as an installed tool.
  - repo: chromasmith/chromasmith-saas-starter
    config: .forge/protocol-config.yaml
    profile: standard
    master_version: "v1.0.0"
    rendered_on: 2026-09-04
    config_hash: f362bf02fcdf61baa61a070137e36a9b90fad35c0329e22b023f516b8c19ce78
    state: current
    files:
      - .forge/protocols/start-protocol.yaml
      - .forge/protocols/end-protocol.yaml
      - .forge/protocol-config.yaml
      - CLAUDE.md
    landed: "commits a296c13 (config), 10bb67e (start), 60c0906 (end), c97a49a (CLAUDE.md), all 2026-09-04; blob SHAs equal; render.py check OK from a fresh forgeflow clone"
    notes: >-
      Second repo rendered (Matt named it 2026-09-04). This repo IS THE TEMPLATE, so its
      rendered files are inherited by every project spawned from it; a child must re-render
      with its own config (repo.name, and drop the build.deploy override once linked to
      Vercel). Private repo: detection ran on a tree skeleton rebuilt from the GitHub listing.
      Detected: single-app, npm-build, supabase, ci_quality_gate (quality-gate.yml), chromaqa
      (chromaqa-check.sh, a real running gate here), tsx_lib_rule; vercel and seogeo off.
      Override build.deploy: none because the template itself is deployed nowhere. The house
      block replaced the placeholder "Session workflow" section of CLAUDE.md (TPL-030).
