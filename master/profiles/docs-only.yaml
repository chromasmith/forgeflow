# ForgeFlow profile: docs-only — a repo with no build and no deploy (specs, protocols, references).
mirror.github_is_complete_mirror: true
build.validation: none
build.deploy: none
execution.local: true
execution.dispatch.enabled: true   # RULING-009: dispatch is available by default; off only when the harness is absent (S-092) or Matt overrides
