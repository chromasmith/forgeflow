# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The other .forge/ files hold state; this holds feel.
# generated: 2026-09-10T22:23:48Z
# session: 2026-09-10 (fourth session — FF-021 on chromasmith/convergible)
written_at_commit: a049f6e3e5f0b1ee695768be712d9b45a05d2463
written_at_utc: 2026-09-10T22:23:48Z
wrap_complete: false
wrap_parts_total: 6

status: |
  FF-021 on chromasmith/convergible is finished: .forge/legal/compliance.yaml (PR #4, 356033a) and the
  Showcase blueprint v6 (PR #5, c82ae59) are at the document standard, each after one review round.
  Master v1.4.2 runs on nine repos; LeadDowser is still pinned at v1.4.1 and was in active edit. No
  unmerged PR from this session in forgeflow or convergible.
  OVERALL POSITION: two of ten repos are into the legacy-document cleanup (convergible done, LeadDowser
  2 of 3 documents), eight have not started, and the protocol system is on one version everywhere except
  LeadDowser.

first_thing: |
  Finish the 2026-09-10 (fourth session) wrap: parts 2-6 in order — .forge/session-state.yaml
  overwrite, .forge/session-history.yaml append, .forge/backlog.yaml edit (FF-024 blocked, FF-026
  added, FF-021 convergible block), .forge/GOTCHAS.yaml append GOTCHA-030..035, final handoff with
  wrap_complete true. Then FF-024 (render LeadDowser to v1.4.2 once it is free).

watch_out_for:
  - "Review every tightening PR's diff, not only its inventory count: PR #5 mapped 392/392 while promoting draft copy to RATIFIED, and PR #4 mapped 64/64 while dropping four current facts (GOTCHA-030, GOTCHA-031). FF-026 puts both checks in the template; it lands before the next tightening dispatch."
  - "Whole-file rewrites of files holding angle-bracket placeholders lose characters even when built from a clone, because the sandbox display shortens them too (GOTCHA-033, 747073a). Compare every returned blob SHA to git hash-object; on a mismatch, diff against the clone first."
  - "Squash-merge runner PRs with an explicit commit_message; a title alone lets GitHub add a claude[bot] Co-authored-by trailer (GOTCHA-034, convergible 356033a)."
  - "Claude Web cannot read convergible's check runs or combined status (GitHub connector 403) or list its Vercel deployments (403); Vercel previews showed Error on docs-only PRs #4 and #5. Diagnosis belongs to a convergible session (GOTCHA-035)."
  - "The v1.4.2 diff checker lived only on a deleted branch (GOTCHA-029): for FF-024 rebuild it or rely on the double render plus the config-hash gate, and say which in the prompt."
  - "Dispatch-start is 640/650 on the maximum knob set; any addition to a block runners read replaces text (P11)."
  - "Loose ends live in the FF-021 block: LeadDowser stale runner branch claude/issue-2-20260910-1815 and inbox files TIGHTEN-LeadDowser-001/002; convergible inbox files TIGHTEN-convergible-001/002, branch wrap-apply-2026-09-09 and four accuracy items for a convergible session."

next_up:
  - "FF-026 (deferred) — tightening template and procedure refinements; after FF-024, before the next FF-021 dispatch."
  - "FF-021 — LeadDowser .forge/design/leaddowser-style-kit.yaml next; not started: seogeo, chromaqa, chromasmith-saas-starter, synclips-platform, chromasync, dv-captain, convergible-sandbox, forgeflow."
  - "FF-025 (deferred) — the propagation diff check becomes a permanent tool; rides the next master change."
  - "FF-010 (parked) — pointer-URL protocols."
  - "P11 prune session for the protocol files, after the cleanups."

north_star: |
  Product-better in a small way: the Showcase blueprint and the legal record that Convergible's build
  reads are lean rule documents with nothing lost (356033a, c82ae59), and review stopped draft owner copy
  from reaching a build as ratified.

tone: |
  A focused cleanup session: one repo named, two runs in parallel, one review round each, two merges.
  Matt wants the whole remaining list kept in view; the next step waits only on LeadDowser being free.
