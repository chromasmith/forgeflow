# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The blueprint holds the design; this holds the feel.
# generated: 2026-09-04T16:20:00Z
# session: 2026-09-04 (step 3a)
# written_at_commit: f7bc3eca844c64808c46b90d7d9f660f585877d5
# written_at_utc: 2026-09-04T16:20:00Z

status: |
  ForgeFlow Master: step 3 is HALF done. 3a landed — render.py, protocol-config.schema.yaml
  (Section 5 fields + the 8 new knobs), profiles/{standard,legacy-local,docs-only}.yaml,
  RENDER_PROCEDURE.md, CHANGELOG.yaml (v0.1.0-build), tests/test_render.py — 31/31 checks
  pass in the sandbox AND from a fresh clone of GitHub (commit 5c66201). The
  LINEAGE_RECONCILIATION gate line was flipped to GATE-PASSED (commit 77ee15c, blob SHA
  proven). RULING-001 recorded in .forge/rulings.yaml and BLUEPRINT.yaml amended to v1.3
  (commit f7bc3ec). NOT written: start-protocol.master.yaml, end-protocol.master.yaml,
  claude-md.house-block.master.md, PROPAGATION_PROCEDURE.md, registered-repos.yaml, the
  legacy banners/README repoint, the S4 ceiling numbers. No repo runs on the master.
  KNOWN DEVIATION: master/BLUEPRINT.yaml Section 5 repo.canonical_checkout example reads
  "C:\Chromasmith\<n>" — should read "<name>"; a one-character transcription slip when the
  file was re-pushed (the GitHub read tool displays "<name>" as "<n>"). Fix it in the first
  commit that next touches the blueprint (the S4 ceiling write); not worth a commit alone.

first_thing: |
  WRITE master/start-protocol.master.yaml. It is transcription, not design: read
  master/LINEAGE_RECONCILIATION.yaml master_block_index (the S-/W- blocks with reader hints
  and render_when fences) and its rules list (every folded LR-nnn names its block), plus
  board_additions_not_from_lineage. Fetch the four convergible-sandbox protocol files from
  GitHub as the primary source text (start-protocol-local.yaml + start-protocol-dispatch.yaml
  for this file). Use the fence syntax render.py parses — one full comment line per fence:
  "# >>> block S-001 | name | reader: all | render_when: always" ... "# <<< end S-001";
  reader tags cw / cc / cc-local / cc-dispatch / all, joined with +; conditions in the tiny
  language in render.py's eval_condition docstring (always, path, not path, path == lit,
  path non-empty, len(path) > N, joined by and/or); placeholders {{repo.name}} etc. must
  resolve against protocol-config.schema.yaml paths. Put "# @reader-order cc-dispatch:
  S-030, S-038, W-003, S-048, S-074" as a directive line before the first block. Keep the
  provisional S-/W-/E- IDs (do NOT renumber to B-nnn — the manifest test and the
  reconciliation both key on them). Write in the sandbox in pieces (a block group per
  create_file), parse-validate the fences by running render.py render against
  tests/fixtures/config-sandbox.yaml with a stub end-protocol.master.yaml if the real one
  is not yet written, wc -l the output, THEN push via the GitHub MCP and prove the blob SHA
  equals git hash-object of the sandbox copy. Budget: this file alone is ~1,300 lines and
  costs two full passes (write + push); do not attempt the end master in the same session.

watch_out_for:
  - "STOP POINT (RULING-001): this effort ends when both masters are written and a
     byte-stable render is proven inside forgeflow. Do NOT render, install, or prepare a
     render for any repo — not convergible-sandbox, not saas-starter — until Matt names it.
     The old 'step 4 = sandbox proof' sequence is gone; do not resurrect it from memory."
  - "Ordering constraint that DID survive: the Convergible FINAL repo is cloned from the
     rendered saas-starter, so saas-starter is never rendered after it."
  - "render.py rejects any non-comment text outside a block fence, duplicate block IDs,
     unknown reader tags, unresolved {{placeholders}}, and the forbidden phrases in
     FORBIDDEN_PHRASES (including the literal string 'render_when' and '{{' inside block
     TEXT — so the master must not quote its own fence syntax in prose; describe it instead)."
  - "Reader semantics in render.py: the cw file includes EVERY condition-true block; the
     cc-dispatch file includes only blocks tagged cc, cc-dispatch or all. Tag blocks
     accordingly — a block tagged cw+cc-dispatch lands in both files."
  - "The LR-026 vs W-001 tension: use the wording in LINEAGE_RECONCILIATION flags —
     planning questions only when the interview trigger fires, led by a recommendation,
     never more than three."
  - "LR-129 (connector cannot read check-run statuses, 403) renders as 'reported' until
     re-tested at a real render."
  - "SEO/GEO start-protocol block: Matt's tool is nearly ready; it goes into the master
     AFTER both masters land and before any render, as a normal master change. Do not
     retrofit it into the start master now unless Matt hands you the text."
  - "The eleven rules SYN/CC2 never had (LR-033, 045, 086, 015, 032, 049, 050, 052, 144,
     148, 140) are in the master unconditionally — the additive first-render diff for
     those repos is expected."
  - "ChromaQA is never installed by a protocol or folded into another task. Do not propose it."
  - "Matt runs concurrent Claude Code sessions. Before pushing to ANY repo other than
     forgeflow, ask whether a CC session is active on it."
  - "Matt's communication rules (blueprint Section 9 I4): four sentences or less,
     recommendation not options, one action item at a time, end every reply with the next
     verb and who holds the ball, state the overall project position at every milestone.
     This session's first close-out read as 'step 3 done' to Matt and had to be corrected —
     say 'the master EXISTS but is not finished' in those words."
  - "YAML traps (bit twice this session): plain scalars with ': ' mid-sentence and commas
     inside flow-mapping values both fail to parse — quote them. The GitHub read tool
     renders the literal text '<name>' as '<n>'; when re-pushing a file you read through
     it, fetch the raw copy in the sandbox and diff before trusting your transcription."
  - "Large files cost two passes (sandbox write, then MCP push) and a 66 KB re-push is
     ~17K tokens; budget the context window before starting a master file."

next_up:
  - "end-protocol.master.yaml (E-001..E-030), then claude-md.house-block.master.md,
     PROPAGATION_PROCEDURE.md, registered-repos.yaml, legacy banners + README repoint,
     wc -l of real renders → write S4 ceilings into the blueprint (fix the <name> slip in
     the same commit), CHANGELOG bump to v1.0.0. Each is deferred, not next; first_thing
     is the only agenda item."

north_star: |
  Did today's work make the product better, or only move the board? Better: the renderer
  is real code with a proof, so the master files that follow will be checked by a machine
  rather than by discipline — that is the whole point of P9.

tone: |
  Transcription session. Head-down, one file, prove it, push it, stop. Matt will say go on
  a clear recommendation; report progress in plain English first. When you close, state
  the overall position plainly: after this file lands the master is TWO-THIRDS written and
  still not finished; no repo runs on it.
