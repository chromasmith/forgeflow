# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The blueprint holds the design; this holds the feel.
# generated: 2026-09-04T22:30:00Z
# session: 2026-09-04 (SEO/GEO 1.0 master change — second tool-driven change to the master)
# written_at_commit: 3ac32c3e24d0b19acf95cb7106d86e9b6a05cbeb
# written_at_utc: 2026-09-04T22:30:00Z

status: |
  ForgeFlow Master is at v0.3.0-build (HEAD a0e2655): the second TOOL-DRIVEN master
  change landed — SEO/GEO 1.0 — in four commits (1b6eb98 CHANGELOG/schema/house
  block/fixture/RENDER_PROCEDURE, ff66b0e render.py, 3ae3f90 start master, a0e2655
  end master), every file's blob SHA matching a fresh fetch (a0e2655), tests 35/35, and a
  fresh clone renders the both-tools fixture byte-identical to the sandbox render.
  What changed: new knob subsystems.seogeo (detected by .seogeo/config.json); NEW
  S-089 (the exact conditional pointer text from seogeo .forge/protocol-pointer.md)
  and S-090 (profiles.json is GENERATED from .seogeo/config.json — one sanctioned
  change, drift = stamp mismatch, no gate reads the posture stamp yet); NEW E-048
  (local-remainder exception to E-040's STOP on a modified profile), E-049
  (.seogeo/.report/ is a LOCAL BUILD ARTIFACT), E-050 (manifest lines), E-051 (file
  behaviors); house block gained two fenced bullets; render.py detects SEO/GEO.
  Existing blocks untouched — every fixture without .seogeo/config.json renders
  byte-identical protocol text to v0.2.0-build (only the config hash moves, because
  the new knob is part of the expanded config). Measured with both tools forced on
  the sandbox-real knob set: start 1564 / dispatch start 616 / end 1354 / dispatch
  end 95 — all under the S4 ceilings (1650/650/1400/150); the end file is closest.
  Still not written (deferred to the session in which Matt names the first repo):
  PROPAGATION_PROCEDURE.md, registered-repos.yaml, legacy banners + README repoint,
  CHANGELOG bump to v1.0.0. Overall position: the unified protocol source exists,
  renders byte-stable, and now describes ChromaQA 7.0 AND SEO/GEO 1.0 correctly;
  NO REPO RUNS ON THE MASTER and RULING-001's stop point still holds; SEO/GEO itself
  is installed nowhere (its own template install is a separate session in the
  seogeo repo, per its INSTALL.md).

first_thing: |
  RENDER chromasmith/seogeo from the master — Matt named it (RULING-002) as the first
  and lowest-risk repo; do it in THIS fresh session after asking in one line whether a
  Claude Code session is active on seogeo. Follow master/RENDER_PROCEDURE.md exactly:
  write seogeo's .forge/protocol-config.yaml (docs-only profile is the likely fit; the
  repo has no build, deploy, database or dispatch), shallow-clone seogeo for detection,
  render twice and diff, check the S4 ceilings, push the rendered files plus the
  CLAUDE.md house block, prove every blob SHA, run render.py check against the fetched
  copies. In the SAME session land the housekeeping bundle: PROPAGATION_PROCEDURE.md,
  legacy banners + README repoint in forgeflow, CHANGELOG bump to v1.0.0, and
  registered-repos.yaml written LAST with seogeo's row. While in seogeo, also make the
  small doc touch already owed there (see watch_out_for item 2). Verify: seogeo holds
  rendered protocols whose header names master v1.0.0 and a config hash, and
  registered-repos.yaml lists it.

watch_out_for:
  - "STOP POINT (RULING-001) still holds for every repo EXCEPT seogeo, which Matt named
     (RULING-002): do NOT render, install, or prepare a render for any other repo — not
     convergible-sandbox, not saas-starter, not chromaqa — until he names it by name.
     A master change is NOT a render."
  - "TWO FOLLOW-UPS OWED IN OTHER REPOS — Matt asked to be reminded and these are NOT
     this repo's work. (1) chromasmith/seogeo, small doc touch: .forge/protocol-pointer.md
     still ends 'the standard does not exist and neither line should be installed yet'
     and its handoff surrounding_context.forgeflow_master still says 'not built or
     installed' — both should now record that the ForgeFlow master carries the fenced
     line (S-089, v0.3.0-build) so a future session does not add it twice; and the
     .seogeo/.report/ directory ships with no .gitignore — the master treats it as a
     LOCAL BUILD ARTIFACT never staged (E-049/E-050/E-051), which is Claude Web's
     recommendation, not a seogeo ruling; seogeo should gitignore it or rule otherwise.
     Ask whether a Claude Code session is active on seogeo before pushing there.
     (2) chromasmith/chromaqa, its own session on Matt's word: stale FOCUSED_BUILD_PROTOCOL
     references in .forge/BLUEPRINT.yaml (noted last session), and the posture-stamp
     gate requests GR-01..GR-05 from seogeo .forge/BLUEPRINT.yaml section 8a. Until
     that lands, S-090 truthfully says no gate reads the posture stamp."
  - "Master-change procedure that worked again this session, in order: clone forgeflow
     (public, plain https clone works) into the sandbox; edit with Python
     replace-and-assert (never regex over the whole file); YAML parse every changed
     file; python3 master/tests/test_render.py (35/35); render every fixture with the OLD
     master (git stash copy) and the NEW master and diff modulo the version stamp and
     renderer bookkeeping lines — protocol text must be identical for every fixture the
     change does not target; render the targeted fixture twice and diff; wc -l against
     the S4 ceilings; git hash-object each file; push through push_files ONE FILE PER
     CALL for the big masters; git fetch and rev-parse origin/main:<path> to prove each
     SHA; finally clone fresh and re-render."
  - "GitHub tool trap (unchanged): create_or_update_file needs the FULL 40-char blob SHA;
     push_files needs no SHA and was used for every landing. The sandbox has no push
     credential — every write goes through the GitHub tool, so a 100 KB master is emitted
     verbatim and MUST be checked by blob SHA afterwards; a mismatch means retype."
  - "A one-line doc edit to master/BLUEPRINT.yaml was made locally and NOT pushed (the
     subsystems knob table at ~line 531 still lists chromaqa as detected by '.chromaqa/
     directory' and has no seogeo line). It is 70 KB for one cosmetic line; land it inside
     the next master change, not as its own push. The schema file is the contract and is
     correct."
  - "New fixture tests/fixtures/config-starter-tools.yaml is ILLUSTRATIVE (template-shaped
     knobs, both tools forced on); it does not claim saas-starter's real detected set.
     tests/fixtures/config-sandbox-real.yaml still has chromaqa and seogeo OFF because the
     sandbox has neither — do not flip them in the fixture; use a temp copy for dispatch
     ceiling measurements."
  - "S-089 and S-090 are reader: all (a dispatched runner creating pages must read the
     standard too). E-048 fences on execution.local AND seogeo AND chromaqa; E-049 on
     execution.local AND seogeo; E-050/E-051 on seogeo. S-014, E-036, E-037, E-040, E-042
     are UNTOUCHED — the seogeo blocks narrow them by stating the exception, never by
     rewriting the ChromaQA-only text."
  - "The exact pointer text in S-089 and the house-block bullet is copied word for word
     from seogeo .forge/protocol-pointer.md; if seogeo changes that text, the master
     follows it (a normal master change), never the other way round."
  - "render.py FORBIDDEN_PHRASES include the fence syntax and placeholder opener as literal
     strings; master prose must describe them, never quote them. Also avoid 'as in
     start-protocol' / 'as in end-protocol' / 'inherited from' / 'see the master'."
  - "YAML traps: plain scalars with ': ' mid-sentence and commas inside flow-mapping values
     both fail — quote them (E-051's action values are quoted for this reason)."
  - "ChromaQA is never installed by a protocol or folded into another task; SEO/GEO is
     never retrofitted into an existing repo (seogeo SC-01). Do not propose either."
  - "Matt runs concurrent Claude Code sessions. Before pushing to ANY repo other than
     forgeflow, ask whether a CC session is active on it."
  - "Matt's communication rules (blueprint Section 9 I4): four sentences or less,
     recommendation not options, one action item at a time, end every reply with the
     next verb and who holds the ball, state the overall project position at every
     milestone."

next_up:
  - "Deferred, not next: the housekeeping bundle (PROPAGATION_PROCEDURE.md, legacy
     banners + README repoint, CHANGELOG bump to v1.0.0, registered-repos.yaml written
     LAST after the first render) lands only in the session in which Matt names a repo."
  - "Deferred, not next: master/BLUEPRINT.yaml knob-table line (seogeo detected_by;
     chromaqa detected_by wording) — inside the next master change."
  - "Deferred, not next: drift_ci stays deferred until after the first real render is
     verified."

north_star: |
  Better: the master now tells a session the truth about SEO/GEO before any repo is
  rendered from it, and resolves the one real conflict (a generated profile vs a
  protected-config rule) in the protocol rather than leaving it for a wrap to trip
  on. Nothing was moved onto any repo's board.

tone: |
  Productive, closed-out. Matt named the tool, asked the right questions about ripple
  and coexistence (answer: none, and the friction was ours, not the tools'), said GO,
  and the change is on GitHub at a0e2655 with proof. He then NAMED seogeo as the first
  render (RULING-002). Open with the one-line position and go straight to the render,
  asking only whether a Claude Code session is active on seogeo.
