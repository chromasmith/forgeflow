# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The other .forge/ files hold state; this holds feel.
# generated: 2026-09-10T20:25:36Z
# session: 2026-09-10 (third session — master v1.4.2, FF-022 + FF-023)
written_at_commit: ca70fd1bcaf17557e115351cc7f183fcbd22c622
written_at_utc: 2026-09-10T20:25:36Z
wrap_complete: true
wrap_parts_total: 7

status: |
  Master v1.4.2 (fd50094) runs on nine repos; registry 2765842 has nine rows current and
  chromasmith/LeadDowser pinned at v1.4.1 because Matt was working in it. v1.4.2 (fd50094) fixed the S-093
  sentence that halted a cold tightening runner, added the W-005 forward-slash line, and refined
  the tightening template with the RULING-032 version rule. No unmerged PR; no transport branch left.
  OVERALL POSITION: the protocol system is on one version everywhere except one pinned repo, and the
  legacy-document cleanup (FF-021) is paused at LeadDowser's style kit, which waits on that repo's
  catch-up render.

first_thing: |
  FF-024 — render chromasmith/LeadDowser to master v1.4.2, the only pinned row in
  master/registered-repos.yaml (2765842). Ask Matt first whether any Claude Code or Claude Web
  session is active on LeadDowser; it was pinned because he was working there. One local Claude Code
  run per PROPAGATION_PROCEDURE step 5 for that one repo: refresh the checkout, double render,
  config-hash gate against its row (efaceee3fa42), expected diff limited to the header stamps, the
  S-093 closing sentence and the W-005 bullet, push, read back six blobs. Verify: the six blobs
  through the connector, then the LeadDowser row set to current v1.4.2 LAST.

watch_out_for:
  - "The v1.4.2 diff checker lived only on the deleted transport branch (blob aae8831b, commit 6cf6e30, now unreachable). For FF-024 either rebuild it — the committed copy with header stamps v1.4.1 to v1.4.2, the S-093 closing sentence swapped and the W-005 bullet inserted must equal the new render byte for byte — or rely on the double render plus the config-hash gate, and say which in the prompt (GOTCHA-029)."
  - "A ChromaQA summary line reading N new warnings logged does not mean .chromaqa/CQA-debt.jsonl changed (GOTCHA-028). Read the ledger with git diff after a push, never the sentence."
  - "Dispatch-start is 640/650 on the maximum knob set (BLUEPRINT current_measurement, v1.4.2). Any addition to a block with reader all or cc-dispatch replaces text (P11)."
  - "LeadDowser loose ends stay in this backlog's FF-021 block: stale runner branch claude/issue-2-20260910-1815, inbox files TIGHTEN-LeadDowser-001/002 await LeadDowser's own wrap, the style kit's code half installs in its P0."
  - "convergible's ChromaQA gate prints one ACCEPTED captain finding (G-20 / SEO-054, Matt's acceptance of 2026-09-07, expiry in its .chromaqa/accepted.json) and pre-existing soft findings on every push; none blocks and none is forgeflow's to act on."

next_up:
  - "FF-021 — LeadDowser .forge/design/leaddowser-style-kit.yaml is the next document, after FF-024."
  - "FF-025 (deferred) — the propagation diff check becomes a permanent tool and PROPAGATION step 3 names it; rides the next master change."
  - "FF-010 (parked) — pointer-URL protocols."
  - "P11 prune session for the protocol files, after the cleanups."

north_star: |
  Product-better: the sentence that stopped a cold runner is fixed in nine repos (fd50094), the
  tightening template carries the version rule and four runner fixes, and a first render now checks
  the north-star document's header.

tone: |
  A short repair session that landed first try (fedf262, fd50094, 2765842): one ruling, one master
  change, one propagation.
  Matt is working in LeadDowser; the next forgeflow session starts with its catch-up render.
