# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The blueprint holds the design; this holds the feel.
# generated: 2026-09-04T20:30:00Z
# session: 2026-09-04 (ChromaQA 7.0 master change — first tool-driven change to the master)
# written_at_commit: 9a8dfb69758b2f7286d7e6e0dc98bbf894a151ec
# written_at_utc: 2026-09-04T20:30:00Z

status: |
  ForgeFlow Master is at v0.2.0-build (HEAD 9a8dfb6): the first TOOL-DRIVEN master
  change landed — ChromaQA 7.0 — in four commits ending at 9a8dfb6, every file proven
  by blob SHA from a fresh fetch, tests 35/35, and a fresh clone renders byte-identical to the
  sandbox render. What changed: S-014 rewritten (debt read from GitHub, profiles.json
  PROTECTED CONFIG, runs.jsonl LOCAL ONLY); S-045 gained the captain-review
  never-fix line; S-071 made repo-neutral; NEW S-087 (pre-push gate rule for local
  code prompts) and S-088 (reading ChromaQA's hard-fail quality-gate CI job);
  E-036/E-037/E-042 carry the three-way file handling; E-040 drops runs.jsonl and
  STOPs on a modified profiles.json; NEW E-047 (local-remainder push-gate block:
  exit 0/1/2, debt+showstoppers ALWAYS PERMITTED modified after a push); house block
  gained one fenced ChromaQA bullet; render.py detects ChromaQA by chromaqa-check.sh.
  Commits: 03cd153 (CHANGELOG, house block, test), 794117d (render.py), 5d9b67c
  (start master, 91 blocks), 9a8dfb6 (end master, 47 blocks). Measured on the real
  sandbox knob set with chromaqa forced on: start 1515 / dispatch start 567 / end
  1293 / dispatch end 91 — all under the S4 ceilings (1650/650/1400/150).
  Still not written (deferred to the session in which Matt names the first repo):
  PROPAGATION_PROCEDURE.md, registered-repos.yaml, legacy banners + README repoint,
  CHANGELOG bump to v1.0.0. Overall position: the unified protocol source exists,
  renders byte-stable, and now describes ChromaQA 7.0 correctly; NO REPO RUNS ON THE
  MASTER and RULING-001's stop point still holds.

first_thing: |
  Matt has a SECOND tool whose install will change what a session must do, and he
  ruled (2026-09-04) that it gets its own fresh session rather than riding on the
  ChromaQA one. When he names it: open its repo, read its README / blueprint /
  installer the way the ChromaQA session did, and deliver a plain-English impact
  assessment BEFORE editing anything — what the masters already say about it, what
  the tool actually does (files it writes, exit codes, CI jobs, config it protects),
  and the exact list of master blocks to add or edit. Then, on Matt's go, ONE master
  change: new blocks fenced on a knob (add the knob to protocol-config.schema.yaml
  with a default if the tool has none), CHANGELOG top entry v0.3.0-build, header
  index lines in both masters, render all four fixtures twice plus the sandbox-real
  set with the knob forced on, check the S4 ceilings, push per file and prove each
  blob SHA. Nothing is rendered into any repo.

watch_out_for:
  - "STOP POINT (RULING-001) still holds: do NOT render, install, or prepare a render for
     any repo — not convergible-sandbox, not saas-starter — until Matt names it by name.
     A master change is NOT a render."
  - "Master-change procedure that worked this session, in order: clone forgeflow into the
     sandbox; edit with Python replace-and-assert (never regex over the whole file); YAML
     parse every changed file; python3 master/tests/test_render.py (35/35); render every
     fixture config twice and diff; render the sandbox-real fixture with the new knob forced
     on via a temp copy (tests/fixtures/config-sandbox-real.yaml has chromaqa OFF because
     the sandbox has no ChromaQA — do not flip it in the fixture); wc -l against the S4
     ceilings; git hash-object each file; push through the GitHub tool ONE FILE PER CALL
     for the big masters; git fetch and rev-parse origin/main:<path> to prove each SHA."
  - "GitHub tool trap: create_or_update_file needs the FULL 40-char blob SHA, not the
     abbreviated one the earlier read printed — a short SHA is rejected as stale. push_files
     needs no SHA and was used for every landing this session."
  - "tests/test_render.py now reads MASTER_VERSION from CHANGELOG.yaml's top entry (it used
     to hard-code v0.1.0-build and went red on the bump). Any future version bump needs no
     test edit."
  - "New blocks S-087, S-088, E-047 all fence on subsystems.chromaqa (S-087/E-047 also on
     execution.local). S-087 says 'next to the CI GATE RULE where that rule is present'
     because S-045 fences on a DIFFERENT knob (ci_quality_gate); the two are detected
     separately and a ChromaQA repo normally has both."
  - "E-047 overrides E-021's VERIFY PUSH check 1 for two paths in ChromaQA repos. E-021
     itself is untouched (it fences on execution.local only). If E-021's audit language is
     ever rewritten, keep E-047's override sentence in step with it."
  - "ChromaQA's own repo carries stale ForgeFlow references (.forge/BLUEPRINT.yaml
     output.files.CQA-debt.jsonl.read_by says 'via FOCUSED_BUILD_PROTOCOL ... after
     extracting repo ZIP', and output_contract.operational_rule_for_agents_and_scope_audits
     says FOCUSED_BUILD_PROTOCOL needs the telemetry exception). The exception now lives in
     the master (E-047). That is a ChromaQA-repo doc fix, NOT this repo's work, and NOT an
     install — mention it to Matt only if he opens the ChromaQA repo."
  - "CLAUDE.md master fences are HTML-comment LINES only; fences do not nest. The ChromaQA
     bullet is one fenced line inside the Protocol files section."
  - "render.py FORBIDDEN_PHRASES include the fence syntax and placeholder opener as literal
     strings; master prose must describe them, never quote them. Also avoid 'as in
     start-protocol' / 'as in end-protocol' / 'inherited from' / 'see the master'."
  - "YAML traps: plain scalars with ': ' mid-sentence and commas inside flow-mapping values
     both fail — quote them. Every pushed .forge/ and master YAML file is parse-validated in
     the sandbox first."
  - "ChromaQA is never installed by a protocol or folded into another task. Do not propose it."
  - "Matt runs concurrent Claude Code sessions. Before pushing to ANY repo other than
     forgeflow, ask whether a CC session is active on it."
  - "Matt's communication rules (blueprint Section 9 I4): four sentences or less,
     recommendation not options, one action item at a time, end every reply with the
     next verb and who holds the ball, state the overall project position at every
     milestone."

next_up:
  - "Deferred, not next: the housekeeping bundle (PROPAGATION_PROCEDURE.md, legacy
     banners + README repoint, CHANGELOG bump to v1.0.0, registered-repos.yaml written
     LAST after the first render) lands only in the session in which Matt names a repo."
  - "Deferred, not next: the SEO/GEO start-protocol block — a normal master change when
     Matt says the tool is ready; may be the same tool as first_thing."
  - "Deferred, not next: a committed fixture config with chromaqa ON (so the ceiling
     measurement for ChromaQA repos is reproducible from tests/fixtures/ instead of a
     temp copy). Cheap; do it inside the next master change, not as its own session."
  - "Deferred, not next: drift_ci stays deferred until after the first real render is
     verified."

north_star: |
  Better: the master now tells a session the truth about ChromaQA 7.0 before any repo
  is rendered from it — a wrong render was avoided rather than repaired. Nothing was moved
  onto any repo's board.

tone: |
  Productive, closed-out. Matt named the priority, ruled on sequencing (one tool per
  session), and the change is on GitHub at 9a8dfb6 with proof. The next move is his: name the second
  tool. Open with a plain one-line position: "master v0.2.0-build describes ChromaQA
  7.0; no repo runs on it yet."
