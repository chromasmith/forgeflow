# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The other .forge/ files hold state; this holds feel.
# generated: 2026-09-06T15:05:00Z
# session: 2026-09-06 (MASTER v1.2.0 FRICTION REPAIR; RULING-013..015; first wrap under the v1.2.0 numbered series)
written_at_commit: 67a91eed046d091f9a2c0ef7b7e38a51604ebc1c
written_at_utc: 2026-09-06T15:05:00Z
wrap_complete: false
wrap_parts_total: 7

status: |
  WRAP IN PROGRESS (part 1 of 7). Master v1.2.0 is landed (54c1293) and forgeflow is re-rendered
  from it (8ec708c, all 14 blob SHAs + render.py check + tests 54/54 re-proven from a fresh clone);
  registry row updated (67a91ee). The other six registered repos still run v1.1.0 (seogeo,
  chromaqa, chromasmith-saas-starter) or v1.0.0 (synclips-platform, chromasync, dv-captain).
  Nothing is unmerged anywhere; the transport branch tmp/v120-apply was deleted by Matt.
  Overall position: the master is repaired at v1.2.0 and runs on ONE repo; the six others lag by design until the one-prompt propagation, and every unregistered repo still runs old protocols under RULING-001.

first_thing: |
  IF YOU ARE READING THIS WITH wrap_complete: false, the 2026-09-06 wrap stopped early. Finish it
  first, under .forge/protocols/end-protocol.yaml, in this order: part 2 session-state (overwrite),
  part 3 session-history (append the 2026-09-06 entry), part 4 backlog (FF-008 completed,
  FF-009/FF-010/FF-011 added, FF-006/FF-007 re-pointed), part 5 GOTCHAS (append 009-011), part 6
  delete .forge/inbox/FF-V120-MASTER.yaml, part 7 rewrite this file with wrap_complete: true.
  The content of every part is derivable from the session-state written in part 2 and this file.
  Only after that: FF-009 (see next_up) is the project's next item.

watch_out_for:
  - "This handoff is the FIRST write of a numbered-series wrap (E-002 v1.2.0); if you see it with wrap_complete: false, parts 2-7 may not have landed — list the commits after written_at_commit whose messages begin 'docs: end session 2026-09-06' to see which parts did."
  - "NEVER carry a large opaque blob (base64/gzip) inside a Claude Web reply — the v1.2.0 first landing attempt failed that way (GOTCHA-009). The working route: push a self-verifying apply script to a temporary branch through the connector, prove its blob SHA, have Claude Code fetch and run it."
  - "Matt's Surface 12 has a main-only pre-push guard (C:\\Users\\chrom\\.config\\git\\hooks\\main-only-guard.sh) that also blocks branch DELETIONS; a transport branch must be deleted in the GitHub browser UI (GOTCHA-010)."
  - "Claude Code on Surface 12 runs PowerShell / Git Bash, not cmd.exe — the master's STEP 0 (S-032/S-036) still emits cmd syntax; the run translates, but FF-011 is the master fix (GOTCHA-011)."

next_up:
  - "FF-009 (ready, P1): propagate v1.2.0 to the other six registered repos by the ONE-PROMPT method in master/PROPAGATION_PROCEDURE.md — Claude Web renders all six in the sandbox first, one Claude Code run pushes; synclips-platform, chromasync and dv-captain still need their claude.yml + protection from Matt or they render dispatch-off with S-092."
  - "FF-007 (ready, P1, after FF-009): convergible-sandbox FIRST render (RULING-012), prerequisites unchanged."
  - "FF-011 (ready, P2): master STEP 0 shell syntax → Git Bash; rides the next master change."
  - "FF-010 (deferred): pointer-URL protocols — revisit only if one-prompt propagation is still too slow (RULING-013)."

north_star: |
  Product-better: the ten friction repairs are in the master text and proven on a real render; the board moved too (registry, backlog), but the protocol a session reads is what changed.

tone: |
  Long repair session with one hard lesson (the corrupted base64 transport) and a clean recovery. Matt wants the six other repos caught up next, in one trip.
