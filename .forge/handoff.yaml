# .forge/handoff.yaml — chromasmith/forgeflow
# Pass-the-baton briefing written BY the outgoing Claude Web session TO the next.
# A WARNING LETTER, not a summary. The blueprint holds the design; this holds the feel.
# generated: 2026-09-04T16:30:00Z
# session: 2026-09-04 (step 3b, second half — end master + step-3 exit proof)
# written_at_commit: ecf77e54141d626f699a00c89ac685c323097958
# written_at_utc: 2026-09-04T16:30:00Z

status: |
  ForgeFlow Master: STEP 3 IS DONE and THE EFFORT IS STOPPED (RULING-001). Both masters
  exist: master/start-protocol.master.yaml (89 blocks, commit ff1c07e, blob dd7a021) and
  master/end-protocol.master.yaml (46 blocks E-001..E-046, commit ecf77e5, blob 1bcf860 —
  equal to git hash-object of the sandbox copy — transcription landed byte-exact at ecf77e5).
  Step-3 exit proof ran inside forgeflow at commit ecf77e5: render.py rendered the three
  fixture profiles AND the real convergible-sandbox knob set (tests/fixtures/config-
  sandbox-real.yaml, config_hash 80024e5a8a5a); two renders with the same --date were
  byte-identical (diff -r clean); every rendered file parses as YAML; render.py check
  reported OK (5 files byte-identical, manifest partition intact); the evidence check
  passed its fixtures (wrap-good 2 verified / 1 reported / 0 unmarked; wrap-bad fails
  on 1 unmarked); tests/test_render.py 31/31. Real-sandbox render sizes: CW start
  1,436 lines, dispatch start 562, CW end 1,207, dispatch end 90 (one block, E-001, by
  design — the wrap is Claude Web's direct write). NO REPO RUNS ON THE MASTER. Not
  written: claude-md.house-block.master.md (C-001 — the one LINEAGE block the masters
  do not hold), PROPAGATION_PROCEDURE.md, registered-repos.yaml, legacy banners +
  README repoint, S4 ceilings, CHANGELOG bump to v1.0.0 (the CHANGELOG top entry still
  says the master files are not written — stale until that bump). Overall position: the
  unified protocol source exists and is proven to render (check OK at ecf77e5); nothing is
  installed anywhere.

first_thing: |
  WAIT. Do nothing on this effort until Matt names a repo (RULING-001). When he names
  one, the opening act of THAT session — before any render — is one housekeeping commit
  in forgeflow: claude-md.house-block.master.md (C-001, from the LR-nnn rules targeting
  C-001 + S-030 and blueprint Section 4 markers), PROPAGATION_PROCEDURE.md,
  registered-repos.yaml (empty registry, written LAST after the first render lands),
  legacy banners + README repoint, S4 ceilings into BLUEPRINT.yaml (fix the Section 5
  "<n>" for "<name>" slip in the same commit), and the CHANGELOG bump to v1.0.0. Then
  RENDER_PROCEDURE.md steps 1-8 for the named repo. If the named repo is
  convergible-sandbox, its two prerequisites come first (repo-rules.yaml from the
  LR-163 items; CC-reports register entry renamed to .forge/inbox/<run-id>.yaml).

watch_out_for:
  - "STOP POINT (RULING-001): this effort is at its stop point. Do NOT render, install,
     or prepare a render for any repo — not convergible-sandbox, not saas-starter —
     until Matt names it by name. A 'might' is not a decision."
  - "S4 CEILING MEASUREMENT: the real-sandbox CW start file is 1,436 lines — ABOVE the
     1,200 the reconciliation recommended (LINEAGE_RECONCILIATION size_measurement).
     Recommendation for the S4 commit: ceilings per READER file, CW 1,500 / dispatch
     800 (about 15 percent headroom over 1,436 and 562); or apply replace-to-add (P11)
     in the start master first. Decide in that commit, not silently."
  - "The end master's E-021 (local remainder) runs BEFORE the docs write when the local
     checkout may hold unpushed work, so its commit SHA can be evidence; the blueprint's
     Section 8 lists it after. The master is the ruling text now; amend the blueprint
     wording in the S4 commit rather than the master."
  - "End master sub-blocks (E-031..E-046) each carry their own top-level YAML key, so
     no rendered file has duplicate keys; the end master has NO mutually exclusive
     variant pairs. The start master does (S-034/S-052, S-081/082/083, S-084/085/086)
     — any new pair anywhere must stay exclusive."
  - "render.py FORBIDDEN_PHRASES include the fence syntax and placeholder opener as
     literal strings; master prose must describe them, never quote them. Also avoid
     'as in start-protocol' / 'as in end-protocol' / 'inherited from' / 'see the master'."
  - "config-sandbox-real.yaml declares the sandbox knobs by hand because the sandbox
     repo is private to the sandbox's git clone (only the MCP can read it); a real
     render uses --tree on a shallow clone via the MCP-fetched files, or declares the
     same overrides. subsystems.chromaqa is false there — the sandbox has no .chromaqa/
     directory (checked via the MCP 2026-09-04)."
  - "The GitHub read tool renders '<name>' as '<n>'; when re-pushing a file read
     through it, fetch the raw copy and diff first. YAML traps: plain scalars with ': '
     mid-sentence and commas inside flow-mapping values both fail — quote them."
  - "SEO/GEO start-protocol block: goes into the master as a normal master change
     (CHANGELOG entry, version bump, re-render of whatever repos are on the master —
     none yet). Do not retrofit into legacy files."
  - "ChromaQA is never installed by a protocol or folded into another task. Do not
     propose it."
  - "Matt runs concurrent Claude Code sessions. Before pushing to ANY repo other than
     forgeflow, ask whether a CC session is active on it."
  - "Matt's communication rules (blueprint Section 9 I4): four sentences or less,
     recommendation not options, one action item at a time, end every reply with the
     next verb and who holds the ball, state the overall project position at every
     milestone."

next_up:
  - "Deferred, not next: the housekeeping bundle named in first_thing lands only when
     Matt names a repo. drift_ci stays deferred until after the first real render is verified."

north_star: |
  Better: the second and last master file exists, and a machine proved that every rule
  from four lineages now renders from one edit point — including a real repo's knob set.
  Nothing was moved onto any repo's board.

tone: |
  Closed-out, waiting. The effort is parked at its stop point on purpose; the next move
  is Matt's, by name. When he makes it, open with the housekeeping commit and a plain
  one-line position: "both masters exist and render; no repo runs on them yet."
