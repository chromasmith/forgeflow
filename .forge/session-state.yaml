# Session state — overwritten each session with current snapshot
session_date: 2026-09-06
project: forgeflow (ForgeFlow Master — unified start/wrap protocols rendered per repo)
milestones:
  - "Session repurposed by Matt from FF-007 to a friction BUG HUNT + REPAIR; scope, internal-only landing and the propagation method recorded as RULING-013, inbox deletion as RULING-014, S-078 retirement as RULING-015 (all @ 6cbdb49)"
  - "Fifteen findings from three 2026-09-05 friction reports (FF-FRICTION-1..4, FF-101..103, FF-R-001..006) de-duplicated into ten repairs and built as master v1.2.0 — landed @ 54c1293 (both masters, house block, schema, BLUEPRINT v1.6, CHANGELOG v1.2.0, PROPAGATION_PROCEDURE rewrite, tests 54/54)"
  - "forgeflow re-rendered from v1.2.0 @ 8ec708c (six files + inbox FF-V120-MASTER in one local Claude Code commit; config_hash 7ccf78dfab2f; start 60 blocks / 1373 lines, end 27 / 1095, dispatch=on; S-003 excluded, S-078 gone, W-005 in); all 14 blob SHAs, render.py check and tests 54/54 re-proven from a fresh clone at 8ec708c"
  - "Registry row for forgeflow updated and master_version_current set to v1.2.0 @ 67a91ee; the other six rows deliberately stay at v1.1.0 / v1.0.0 (RULING-013)"
  - "Matt's pointer-URL idea (repos hold a link to one master file) evaluated and declined; propagation redesigned as ONE local Claude Code run (PROPAGATION_PROCEDURE.md v1.2.0, BLUEPRINT delivery_rule)"
decisions:
  - decision: "The wrap is a NUMBERED SERIES of single-file SHA-guarded commits with the handoff written first (wrap_complete: false) and last (wrap_complete: true); the start protocol ignores the wrap's own commits for staleness and finishes an unfinished wrap before project work"
    rationale: "The GitHub connector has no patch, no append and no delete inside a multi-file commit, and its multi-file write takes no SHA guard — six of the fifteen findings were this one root cause."
    impact: "E-002/E-014/E-030 and S-010 rewritten; this session's own wrap is the first run of the method."
  - decision: "Consumed inbox files are DELETED at the wrap, not archived (RULING-014); S-078 Desktop copy retired (RULING-015)"
    rationale: "Git history already holds every inbox file; the Desktop copy contradicted the NO MEMORY WRITES clause and was overruled by every run."
    impact: "Roughly two-thirds fewer wrap operations; nothing is ever written outside the repository tree."
  - decision: "Large master changes travel to Matt's machine as a self-verifying APPLY SCRIPT on a temporary branch pushed through the connector — never as a base64/gzip blob inside a Claude Web reply (GOTCHA-009)"
    rationale: "The first v1.2.0 landing attempt arrived corrupted: a token model cannot emit 31 KB of base64 reliably; a Python script of exact-match replacements with blob-SHA checks is prose the connector carries intact."
    impact: "Route proven end-to-end from a fresh clone before the run; the run fetched blob 82288e6, reproduced all eight SHAs, and landed 54c1293 / 8ec708c first try."
  - decision: "Propagation of a master change is ONE local Claude Code run over every registered repo, against sandbox-proven blob SHAs (RULING-013); the pointer-URL alternative is parked"
    rationale: "Matt named rollout slowness as the biggest friction; the slow part was the connector's one-file-per-call landing, not the design."
    impact: "FF-009 is the first run of the method; the board's PR-per-repo rule is superseded in BLUEPRINT v1.6."
blockers: []
next_steps:
  - priority: high
    task: "FF-009 — propagate master v1.2.0 to the other six registered repos by the one-prompt method in master/PROPAGATION_PROCEDURE.md: Claude Web renders all six in the sandbox (private repos from tree skeletons), records blob SHAs, asks the ONE batch concurrency question, then a single local Claude Code run renders, verifies against those SHAs, commits one named-file commit per repo and pushes; synclips-platform, chromasync and dv-captain render dispatch-off (S-092) unless Matt installs claude.yml + protection first; registry rows written last by Claude Web"
context: |
  MASTER: v1.2.0 complete on GitHub (54c1293); 96 start blocks (S-078 retired, W-005 new) and 51
  end blocks; tests 54/54 from a fresh clone. Ceilings on the real sandbox knob set: start
  1595/1650 (55 lines of headroom — the next start-protocol addition must replace something, P11),
  dispatch start 588/650, end 1280/1400, dispatch end 95/150. RENDERED: forgeflow only (8ec708c).
  LAGGING BY DESIGN: seogeo, chromaqa, chromasmith-saas-starter (v1.1.0, harness installed);
  synclips-platform, chromasync, dv-captain (v1.0.0, secret set, no claude.yml, no protection).
  convergible-sandbox (FF-007) is still an unrendered first render with its prerequisites unchanged.
  LANDING ROUTES that work: connector create_or_update_file for files under ~80 KB (retry on an
  aborted call, prove by blob SHA); apply script on a temporary branch + local Claude Code for
  anything larger. Matt's Surface 12 has a main-only pre-push guard that also blocks branch
  deletions — transport branches are deleted in the GitHub browser UI (the connector has no
  branch-delete tool). Claude Code there runs PowerShell / Git Bash, not cmd — FF-011. First
  halted Claude Code run (corrupted base64) left no inbox file; its report was pasted into chat
  and consumed here. Deferred, not next: forgeflow CLAUDE.md above the house block is still the
  unfilled template; drift_ci; the approval code and canonical paths sit in a PUBLIC repo (Matt
  has not asked for that to change).
